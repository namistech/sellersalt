node_modules/
.next/
.env
.env.local
.env.*.local
*.log
.DS_Store
dist/
*.tsbuildinfo

# Local-disk storage fallback (src/lib/storage/local-storage.ts) — only used
# when no S3/R2 credentials are configured; ephemeral, container-local in
# production, never meant to be committed.
public/uploads/
backups/

